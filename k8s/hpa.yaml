apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: go-gist-signal-server
  namespace: go-gist-signal-server
  labels:
    app: go-gist-signal-server
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: go-gist-signal-server
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
